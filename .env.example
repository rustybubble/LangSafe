# ============================================================
# LangSafe — Environment Variables
# Copy to .env.local and fill in your values
# ============================================================

# --- Core Services (required) ---
ELASTIC_URL=https://your-cluster.es.us-central1.gcp.cloud.es.io:443
ELASTIC_API_KEY=your_elastic_api_key
JINA_API_KEY=your_jina_api_key              # Used for embeddings (v3) + reranker (v2)

# --- Featherless.ai (LingHacks sponsor integration) ---
# OpenAI-compatible Ask, Studio, discovery planning, extraction, and transcript correction.
FEATHERLESS_API_KEY=your_featherless_api_key
FEATHERLESS_MODEL=Qwen/Qwen2.5-7B-Instruct

# --- WebSocket / Backend Server ---
# URL where the standalone Express + Socket.io server is running
NEXT_PUBLIC_WS_URL=http://localhost:3001
WS_PORT=3001

# --- ML Service (Python audio pipeline) ---
ML_SERVICE_URL=http://localhost:3003

# --- Browserbase / Stagehand (web crawling) ---
BROWSERBASE_API_KEY=your_browserbase_api_key
BROWSERBASE_PROJECT_ID=your_browserbase_project_id
# Set to BROWSERBASE for cloud, omit for LOCAL headless
# STAGEHAND_ENV=BROWSERBASE

# --- Cloudflare (R2 storage + KV cache) ---
CLOUDFLARE_WORKER_URL=https://your-worker.your-subdomain.workers.dev

# --- HeyGen (avatar pronunciation videos) ---
HEYGEN_API_KEY=your_heygen_api_key

# --- BrightData (MCP tools + optional legacy proxy) ---
# API token for BrightData MCP (search, scrape, CAPTCHA bypass)
# Free tier: 5,000 requests/month
# Get token: https://brightdata.com/cp/api_access_tokens
BRIGHTDATA_API_TOKEN=your_brightdata_api_token

# Legacy proxy (optional, used as fallback if API token not set):
# BRIGHTDATA_PROXY_HOST=brd.superproxy.io
# BRIGHTDATA_PROXY_PORT=22225
# BRIGHTDATA_PROXY_USERNAME=your_username
# BRIGHTDATA_PROXY_PASSWORD=your_password

# --- Poke (iMessage notifications) ---
# POKE_API_KEY=your_poke_api_key
# POKE_BASE_URL=https://api.pokemcp.com

# --- Frontend (client-side, must start with NEXT_PUBLIC_) ---
# Override API base URL if needed (defaults to relative)
# NEXT_PUBLIC_API_URL=
